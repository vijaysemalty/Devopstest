---
 - name: Working with AWS EC2 Instance
   hosts: localhost
   connection: local
   gather_facts: false
   tasks:
     - name: Create ec2 instance
       ec2:
         instance_type: t2.micro
         image: ami-0b0ea68c435eb488d
         count: 1
         key_name: mckmasteransible
         region: us-east-1
         tags:
           name:  myfirstansible
